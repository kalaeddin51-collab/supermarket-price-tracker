version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Local dev uses SQLite (DATABASE_URL in .env points to ./prices.db)
    volumes:
      - .:/app  # mount source so local changes reflect without rebuild
    restart: unless-stopped
