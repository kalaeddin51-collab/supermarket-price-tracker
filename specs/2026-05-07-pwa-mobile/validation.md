# Phase 8 — Validation

- [ ] `GET /static/manifest.json` returns 200 with correct Content-Type
- [ ] Chrome DevTools → Application → Manifest shows app name, icons, theme colour
- [ ] "Add to Home Screen" option appears in Chrome mobile (after manifest + HTTPS)
- [ ] Bottom tab bar visible at viewport < 640px, hidden at ≥ 640px
- [ ] "More" sheet opens/closes correctly; links work
- [ ] Active tab correctly highlighted based on current page
- [ ] Content not hidden under tab bar on short pages (pb-20 applied)
- [ ] Top nav icons/labels still render correctly on desktop
- [ ] App imports successfully (`python -c "import app.main"`)
